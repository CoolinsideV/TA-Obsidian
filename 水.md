# 猪都看得懂的水体Shader渲染

写得好，那是马尔代夫清澈见底的玻璃海；写得不好，那是一锅翻滚的绿豆沙。

---

**水其实是“一面能透视的凹凸镜子”**。


## 水体到底是什么？

在计算机图形学中，我们不能真的去模拟流体动力学（算不起）。我们渲染的水面，其实就是一个**扁平的Mesh（网格）**。为了让这个扁平的网格看起来像水，我们需要欺骗玩家的眼睛，主要从以下 **5个大类** 着手：

1. **水体深度 (Depth)：** 水有多深？（决定了岸边和深水区的颜色差异、白沫的生成位置）。

2. **法线与波浪扭曲 (Normal & Distortion)：** 水面不是平的，波光粼粼怎么做？

3. **折射与颜色 (Refraction & Color)：** 透过水面看水底，水底的石头会扭曲，且会染上水的颜色。

4. **焦散 (Caustics)：** 阳光透过波浪聚焦在水底的漂亮光纹。

5. **反射与高光 (Reflection & Specular)：** 水面反射天空，太阳在水面上形成的高光。

6. **岸边白沫 (Foam)：** 水和岸边交界处的物理交互。

---

接下来，我们逐一拆解这些效果的代码实现。

## 1. 水体深度 (Water Depth) 


要知道水的深浅，我们需要知道两个数据：**水底在哪里（场景深度：`_CameraDepthTexture`）**，以及**水面在哪里（当前片元视线深度：```o.positionVS = TransformWorldToView(o.positionWS);

```

在URP中，我们可以直接采样 `_CameraDepthTexture` 获取不透明物体（也就是水底地形）的深度。

而  o.positionVS = TransformWorldToView(o.positionWS);  // 顶点→观察空间
即：该片元的观察深度 = -positionVS.z (等价于 positionCS.w)
```


```hlsl
// 屏幕空间下的UV坐标
float2 screenUV = i.positionCS.xy / _ScreenParams.xy;

// 1. 获取场景中不透明物体（水底）的深度
half depthTex = SAMPLE_TEXTURE2D(_CameraDepthTexture, sampler_CameraDepthTexture, screenUV).r;
// 2. 将非线性的深度值转换为视口空间下的线性深度（距离摄像机有多远）
half depthScene = LinearEyeDepth(depthTex, _ZBufferParams);

// 3. 计算水深：场景深度 + 水面视空间Z值
half depthWater = depthScene + i.positionVS.z; 
depthWater *= atten; // 乘以衰减系数调整深浅变化速度
```

> **变量与逻辑详解：**
> 
> - `depthScene`：是从深度图解算出来的**水底**到相机的真实物理距离（正值）。
>    
> - `i.positionVS.z`：是**水面网格**在观察空间下的Z值。在Unity中，相机看向的是负Z轴，所以这个值是**负数**。
>    
> - **为什么是加法？** 因为数学上：$\text{水深} = \text{水底距离} - \text{水面距离}$。由于Unity视空间Z是负的，所以 $depthScene + i.positionVS.z$ 就等同于 $depthScene - \vert{}i.positionVS.z\vert{}$，完美得出水的厚度
>    

---

## 2. 法线与波浪扭曲 (Normal & Distortion) —— 让水动起来


水面有波纹，波纹会改变水表面的朝向（法线）。我们通常用两张法线贴图，朝着不同方向滚动并混合，来模拟复杂随机的波浪。有了波浪法线，我们就可以扭曲视线（Distortion），产生折射的错觉。


```hlsl
// 在顶点着色器中，我们已经计算了两个不同方向和速度滚动的UV：
// o.normalUV.xy 和 o.normalUV.zw

// 1. 采样两层法线并相乘混合
half4 normalTex01 = SAMPLE_TEXTURE2D(_NormalTex, sampler_NormalTex, i.normalUV.xy);
half4 normalTex02 = SAMPLE_TEXTURE2D(_NormalTex, sampler_NormalTex, i.normalUV.zw);
half4 normalTex = normalTex01 * normalTex02;

// 2. 乘以扭曲强度，提取出用于UV偏移的向量
half3 normal = normalTex.xyz * distort;

// 3. 将屏幕UV加上法线的xy偏移量，得到扭曲后的UV
float2 distortUV = screenUV + normal.xy;
```


> - `normalTex01 * normalTex02`：两张波浪图错位叠加，打破单张贴图的重复感。
>    
> - `distortUV`：我们不用原本的屏幕UV去采样水底，而是加上了法线的扰动。法线偏向哪，我们就去采样旁边的像素，这就是“折射扭曲”的假动作原理。

---
## 3. 折射与颜色 (Refraction & Color) —— 有色玻璃


水不仅会扭曲背后的画面，还会吸收光线。水越深，吸收的光越多，颜色越深。我们用刚才计算的“水深”去采样一张颜色渐变图（Ramp Map），再混合扭曲后的水底画面。


```hlsl
// 1. 防穿帮处理：用扭曲后的UV重新算一次深度
half depthDistortTex = SAMPLE_TEXTURE2D(_CameraDepthTexture, sampler_CameraDepthTexture, distortUV).r;
half depthDistortScene = LinearEyeDepth(depthDistortTex, _ZBufferParams);
half depthDistortWater = depthDistortScene + i.positionVS.z;

float2 opaqueUV = distortUV;
// 【关键防穿帮】如果扭曲后的水深<0，说明我们扭曲得太厉害，取到了水面以上的岸边像素！
// 这时候强制退回无扭曲的屏幕UV。
if(depthDistortWater < 0) opaqueUV = screenUV; 

// 2. 采样水底画面（折射图）
half4 opaqueTex = SAMPLE_TEXTURE2D(_CameraOpaqueTexture, sampler_CameraOpaqueTexture, opaqueUV);

// 3. 采样颜色渐变图（横坐标是水深）
half4 rampTex01 = SAMPLE_TEXTURE2D(_RampTexture, sampler_RampTexture, float2(depthWater, 1)); // 表层水色
half4 rampTex02 = SAMPLE_TEXTURE2D(_RampTexture, sampler_RampTexture, float2(depthWater, 0)); // 水底透色
```

> - `_CameraOpaqueTexture`：URP提供的当前屏幕的不透明渲染结果（也就是没有水的干世界）。
>     
> - 防穿帮逻辑极其优雅：水波扭曲有时会把水面以上的石头倒影拽进水里（因为屏幕贴图是扁平的）。通过判断 `depthDistortWater < 0`，我们把这些“越界”的采样强行拉回原地。

---
## 4. 焦散 (Caustics) —— 光影


焦散是光线经过波浪折射后，聚焦在水底形成的网格状光斑。要让焦散贴合水底地形，我们需要把水底像素在**屏幕上的2D坐标**，反推回它在**3D世界中的真实坐标**（World Position Reconstruction）。


```hlsl
// 1. 利用相似三角形，从水面视空间xy推导水底视空间xy
float4 depthVS = 1;
depthVS.xy = i.positionVS.xy * depthDistortScene / -i.positionVS.z;
depthVS.z = depthDistortScene;

// 2. 将视空间坐标转换回世界空间坐标
float3 depthWS = mul(unity_CameraToWorld, depthVS).xyz;

// 3. 用水底的XZ坐标作为UV，加上时间偏移，采样两层焦散图
float2 causticUV01 = depthWS.xz * _CausticTex_ST.xy + depthWS.y * 0.2 + i.timeOffset;
float2 causticUV02 = depthWS.xz * _CausticTex_ST.xy + depthWS.y * 0.1 + i.timeOffset * float2(-1.07, 1.43);
half4 causticTex01 = SAMPLE_TEXTURE2D(_CausticTex, sampler_CausticTex, causticUV01);
half4 causticTex02 = SAMPLE_TEXTURE2D(_CausticTex, sampler_CausticTex, causticUV02);

// 4. 取两层焦散的最小值（让光斑线条更细锐）
half4 caustic = min(causticTex01, causticTex02) * causticIntensity;
```

> 
> $\frac{X_{scene}}{Z_{scene}} = \frac{X_{water}}{Z_{water}}$ （视空间下射线沿相机出发，形成相似三角形）。
> 
> 因此水底视空间 $X_{scene} = X_{water} \times \frac{Z_{scene}}{Z_{water}}$。代码里的 `depthVS.xy` 就是利用这个原理，精准还原了水底那块石头的3D坐标。然后直接拿 `depthWS.xz` 贴上焦散贴图，光斑就完美“印”在石头上

---
## 5. 高光与反射 (Specular & Reflection) —— 波光粼粼的镜面


水面不仅透明，它还反光。这里用了经典的 **Blinn-Phong光照模型** 算太阳高光，用 **Cubemap** 算天空反射，并结合 **菲涅尔效应（Fresnel）**（视线越平齐水面，反光越强）。


```hlsl
// --- 高光 (Blinn-Phong) ---
// 根据法线扰动值混合几何法线和贴图法线
half3 N = lerp(normalize(i.normalWS), normalTex.xyz, specularDistort); 
half3 L = light.direction; // 光方向
half3 V = normalize(_WorldSpaceCameraPos.xyz - i.positionWS.xyz); // 视线方向
half3 H = normalize(L + V); // 半角向量

// 高光公式：(N·H)^Smoothness
half4 specular = _SpecularColor * specularIntensity * pow(saturate(dot(N, H)), specularSmoothness);

// --- 反射与菲涅尔 ---
// 计算反射向量并采样Cubemap
half3 reflectionUV = reflect(-V, normal);
half4 reflectionTex = SAMPLE_TEXTURECUBE(_ReflectionTex, sampler_ReflectionTex, reflectionUV);

// 菲涅尔效应近似公式：F = (1 - N·V)^3
half fresnel = pow(1 - saturate(dot(i.normalWS, V)), 3);
half4 reflection = reflectionTex * fresnel;
```


> - `dot(N, H)`：表面法线和半角向量越接近，说明光线刚好反射进眼睛，高光越亮。
>    
> - `fresnel`：当 `dot(N, V)` 趋近于0（你趴在水边看水），Fresnel值趋近1，反射最强烈。这也是为什么远处的海面看起来像镜子。

---
## 6. 岸边白沫 (Foam) —— 柔软的边缘交接


水和岸边物体交界的地方，会产生浪花白沫。怎么判断是不是交界处？**水深非常浅的地方，就是交界处**。


```
// 1. 根据水深计算一个判断泡沫的阈值，水越深这个值越大
half foamWidth = depthWater * foamRange;

// 2. 采样一张带噪点的泡沫贴图
half foamTex = SAMPLE_TEXTURE2D(_FoamTex, sampler_FoamTex, i.uv.xy).r;
foamTex = pow(abs(foamTex), foamNoise); // 控制噪点的对比度

// 3. Step函数：当泡沫图的值 > 水深相关的阈值时，返回1，否则0
half foamMask = step(foamWidth, foamTex);
half4 foam = foamMask * _FoamColor;
```

> `step(a, b)` 函数是Shader里的“if语句”平替（$b \ge a$ 返回1，否则0）。
> 
> 离岸边越近，`depthWater` 越接近0，`foamWidth` 也接近0。此时 `foamTex` 里的噪波点很容易大于它，输出白沫。随着水变深，阈值变大，只有噪波最强的地方才能生成白沫，形成自然的破碎感。

## 组装

最后一步，把上面所有算好的元素按照一定逻辑加起来：


```
half4 c = half4(0,0,0,1);

// 表层部分：水本色 + 高光反射叠加 + 白沫
c += rampTex01 * lightness;
c += specular * reflection;
c += foam;

// 水底部分：(扭曲的水底画面 + 焦散) * 水底透色遮罩
c += (opaqueTex + caustic * lightness) * rampTex02;

return c;
```

### 总结

- 用 **深度差** 判断远近；

- 用 **UV偏移** 伪造折射；

- 用 **相似三角形** 找回世界坐标画焦散；

- 用 **点乘算夹角** 搞定高光和菲涅尔。

