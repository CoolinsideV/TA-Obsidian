
首先我们要根据实际项目需求来确定cs具体落实的功能，在这个项目中cs负责计算 **锚点与变换**（GPU Instancing / 间接绘制）

当然cs还有其他用处：
- 计算网格的形变与模拟（顶点动画 / 物理模拟）
- 直接计算所有的顶点与面（纯程序化网格生成）


---

## 第一步：定点

### 1）C#端： 分配显存并设置间距 —— CPU计算需要的线程组数量并发出Dispatch

1.定义

- 这一端的声明数据要和Cs端的一致
![[草地-001.png|425]]
![[草地-002.png|579]]

- 创建追加缓冲区 (Append Buffer)
  在传统的 CPU 模式下，判断一万根草是否在屏幕视野内是非常耗时的操作
![[草地-003.png|496]]
	  在 `InitializeComputeBuffers` 中，使用 `ComputeBufferType.Append`
	  不是在 GPU 端预先固定好一个数组的每个位置，而是给 GPU 一个空列表，让算好的线程自己把数据“推 (Append)”进去。可以简单地理解成.Append()列表里的就是可以看见的草。声明动态追加缓冲区之后我们需要：
	  
  1.初始化计算缓冲区![[草地-004.png]]
	  a.   普通缓冲区只需要数量和步长，但追加缓冲区必须在构造时传入 `ComputeBufferType.Append` 标志，这样 GPU 才会为它分配一个隐藏的计数器（Counter）,`sizeof(float) * 3` 是在计算 **ComputeBuffer 中单个数据元素所占用的字节大小（也就是“步长”/Stride）**。
	  b.   Append Buffer 是基于内部计数器来决定下一个数据写入位置的，刚创建时内部计数器的状态是不确定的，必须手动初始化为 0

  2.将 Buffer 绑定给 Compute Shader
![[草地-005.png|567]]
在执行派发前，必须明确告诉 GPU 将数据追加到哪个内存槽位。使用 `computeShader.SetBuffer(kernelID, bufferID, appendBuffer);` 将其绑定
  
  3.每次 Dispatch 前重置计数器 (`SetCounterValue`)
![[草地-006.png|546]]
如果你在 `Update` 中每帧调用 Compute Shader 生成草，必须在每次派发（Dispatch）前调用 `buffer.SetCounterValue(0);`。如果不重置，GPU 会接着上一帧的末尾继续追加数据，导致缓冲区迅速爆满并溢出崩溃
   
  4.生命周期结束时显式释放内存 (`Dispose`)![[草地-007.png|536]]
  由于 ComputeBuffer 直接向系统显存申请了空间，它不受 C# 垃圾回收（GC）的管理。当脚本销毁（如 `OnDestroy`）或禁用时，必须显式调用 `buffer.Dispose();` 来释放显存，否则会导致严重的内存泄漏。
  

- 分配线程组
  int threadGroupsX = Mathf.CeilToInt(resolution / 8f); 线程组是按 8 的倍数打包的，如果设定的 `resolution`（网格分辨率）是 100，`100 / 8 = 12.5`，`Mathf.CeilToInt` 向上取整会得到 13。这确保了派发的线程总数（104×104）一定能够完全覆盖你需要的 100×100 网格。

- 每帧清零计数器
  在 `Update` 每一帧调度前，调用了 `grassBladesBuffer.SetCounterValue(0)`。因为使用的是 Append 缓冲区，如果不每帧清空，新的草地数据会无休止地跟在上一帧的数据后面追加，**瞬间撑爆显存**

- 防内存泄漏
  在 `OnDestroy` 阶段显式调用 `buffer.Dispose()` 是非常规范的写法，ComputeBuffer 是直接在显存上开辟的空间，Unity 的垃圾回收机制（GC）管不到它，必须手动释放



### 2）Cs端： GPU执行2D网格到3D锚点坐标的转换 —— 将有效坐标Append进缓冲区

1.定义（数据结构对齐）

![[草地-008.png|341]]
- 定义结构体定义单根草叶属性
- 动态追加结构缓冲区
- 声明：网格密度、间距调节 


2.线程组

![[草地-009.png]]
- 剔除越界线程：
	- 核函数：防止线程越界，计算每根草在3D空间中的具体位置
	- 核心逻辑转换：把一个 2D 的逻辑网格转换成 3D 的物理位置，公式就是： `(逻辑上的横坐标, 逻辑上的纵坐标)` ➡️ `(3D的X轴, 0, 3D的Z轴)` 这就是为什么 `id.x` 分给了 X，`id.y` 分给了 Z，而中间留给 Y 的是 0。
- 装填数据
	- 算出草地锚点在 XZ 平面上的 3D 坐标后，通过 `_GrassBlades.Append(blade)`，每个合格的线程都会把算好的草地实例扔进 C# 准备好的那个大列表中




---

## 第二步：抖起来

### 1）C#端：

1.增加一个带滑动条控制的浮点数![[草地-010.png|497]]
2.在底部的 `static readonly int` 列表中，增加一个 `jitterStrengthID`
3.在 `SetupComputeShader` 函数中（每帧更新），把面板上调好的值真正发送给 Compute Shader 里的对应变量

### 2）Cs端：

1.**接收 CPU 数据**：在文件顶部声明了同名变量，用来接住 C# 传过来的扰动强度。

2.引入哈希函数 (生成随机数)![[草地-011.jpg|517]]![[草地-012.png|519]]
给它一个固定的 2D 坐标（种子）,**返回一个恒定不变但看起来毫无规律的 0 到 1 之间的随机小数**

3.应用坐标偏移
![[草地-013.png|587]]
	a.提取随机数
	b.映射并缩放
	c.施加偏移


---

## 第三步：画出来

![[草地-014.png|255]]![[草地-015.png|176]]![[草地-016.png|205]]![[草地-017.png|252]]![[草地-018.png|264]]

在这一步我们需要先明确，画草要拆解成两步骤：

- 画什么？—— 模型图纸（Meshdata/几何数据）
  `GrassMesh.cs` 里硬编码捏出来的那一堆 `Vector3` 数组，以及后来被拆解装进 `meshPositionsBuffer`、`meshTrianglesBuffer` 里的数据

- 画在哪儿？—— 坐标名单（Instance Data / 实例化数据）
  Compute Shader 里那个负责不断接收数据的 `AppendStructuredBuffer _GrassBlades`

- 画的东西怎么才能被看见？ —— 摄像机 + 材质（**没有 MeshRenderer，材质必须自己传**）![[草地-019.png|270]]

### 现在只动C#端：

通俗的解释这一阶段在干嘛：给 GPU 递上一个“草叶片”的 3D 模型图纸，告诉 GPU“请拿着这张图纸，去 `_GrassBlades` 列表里的每一个坐标点上，给我照着图纸画一根草
![[草地-020.png|513]]

1.准备“草片”模型数据 (引入 `GrassMesh.cs`)，并拆解“模型图纸”

- 1）要画草，得先有草的形状。这次引入了一个专门生成网格的类 `GrassMesh`

- 2）然后在 `SetupMeshBuffers` 中，通过 `GrassMesh.CreateHighLODMesh()` 获取一根草的模型实例。这就相当于你在内存里拿到了一份完整的草片 3D 打印说明书。
![[草地-021.png|489]]

![[草地-022.png|312]]

- 3）拆解模型图纸 ![[草地-023.png]]
	- 1. 提取零件（上半部分）
	- 2. 通过四个 `CreateBuffer` 操作，在 GPU 显存里开辟了四个ComputeBuffer，把刚才拆出来的零件分门别类地装进去。后面的 `sizeof(...)` 就是在精准定义每个零件占用的字节大小，例如一个三维坐标占 12 个字节（`sizeof(float) * 3`）


2.引入 Indirect Arguments Buffer（间接参数缓冲区）

在传统的 Unity 开发中，你只要给物体挂上 `MeshFilter` 和 `MeshRenderer`，Unity 就会自动帮你把模型交给显卡去画。但因为我们现在用的是极度硬核的 `DrawProceduralIndirect`（纯 GPU 驱动渲染  ！！！！！**Unity 的传统偷懒组件全部失效了** ！！！！ 此时的 GPU 只知道要去哪些坐标点种草（这得益于之前的 Append 列表），却根本不知道“一根草”到底长什么样  ！！！！

- 1）在 `InitializeComputeBuffers` 中新建了一个 `argsBuffer`，类型是 `IndirectArguments`
![[草地-024.png]]
	每个实例的索引数量，实例数量，开始的索引位置，开始的顶点位置，开始的实例位置


- 2）初始化参数 （`argsBuffer` 是专门用来启动 GPU 间接绘制的命令单）
![[草地-025.png]]
- 参数 1：`meshTrianglesBuffer.count`（画一根草需要读取多少个三角形顶点）—— 已经确定了的索引数量
- 参数 2：`0`（要画几根草？先占位，等下用 CopyCount 填入真实数字）
- 参数 3：`0`（从第几个点开始画）
- 参数 4：`0`（从第几根草开始画）


3.为不同的Buffer创建通用的模板函数

- 1）**计算着色器要往一个列表里动态追加数据** —— 在`AppendStructuredBuffer<GrassBlade> _GrassBlades`中，GPU 每帧并行算出几千根草的位置，我们需要追加写进去。

- 2） **绘制参数也要由 GPU 决定** —— 有多少根草，只有 GPU 算完才知道，所以 `DrawProceduralIndirect` 必须从一个 buffer 里读参数，不能在 C# 里传数字。
![[草地-026.png]]


4.**把 CPU 侧的 `Mesh` 顶点数据，搬成 GPU 侧可以被顶点着色器按 vertexID 索引的 StructuredBuffer**
![[草地-027.png]]
- 普通的 `MeshRenderer` / `Graphics.DrawMesh` 画一个 mesh 时，Unity 会自动把 mesh 的顶点流按语义喂给 shader 的 `Attributes` 结构体——你写 `float3 positionOS : POSITION`，引擎负责绑定。
- 但这里用的是 `Graphics.DrawProceduralIndirect`，它**没有 mesh 参数**，不绑定任何顶点缓冲。所以顶点着色器没法再用语义接数据，只能靠 `SV_VertexID` 自己去别处查：

```hlsl
struct Attributes
{
    uint vertexID : SV_VertexID;
    uint instanceID : SV_InstanceID;
};
```

- `CreateComputeBuffersForMesh` 就是给 `SV_VertexID` 准备"别处"——读取权在：

```hlsl
StructuredBuffer<int>    Triangles;
StructuredBuffer<float4> Colors;
StructuredBuffer<float2> Uvs;
```


5.每帧获取真实草的数量（CopyCount）

- 1）上一帧我们知道，由于加入了 `if` 边界检查（或者未来的剔除逻辑），到底有多少根合格的草被 `Append` 进列表里，但是CPU 其实是不知道的（都在显存里）。这行代码让 GPU 把 `grassBladesBuffer` 里**当前真实的草数量（计数器）**，悄悄地复制到了 `argsBuffer` 数组的第二个位置（即实例总数）。
![[草地-028.png]]

6.执行间接绘制 (DrawProceduralIndirect)

- 1）写在 `LateUpdate` 的 `RenderGrass` 方法
	  为什么不用 `Update`？因为 C# 是在 `Update` 里通知 Compute Shader 去算坐标的。把画画的指令写在 `LateUpdate`（晚于普通 Update 执行），是为了**绝对保证 GPU 已经把本帧所有的草坐标都算完了、名单是最新的**，然后再下笔去画，防止画面出现撕裂或少画了一帧的现象
- 2）因为**没有传 Mesh 对象**。正常情况下 Unity 会自动把 Mesh 的 vertex buffer / index buffer 绑到管线里，但现在 GPU 手上什么都没有，所以只能自己把 `mesh.triangles / vertices / colors / uv` 各拷一份进 `StructuredBuffer`，然后在顶点着色器里用 `SV_VertexID` 手动查表
![[草地-029.png]]
这个自己再找一下资料吧....留个坑   


为草叶创建专门投射阴影的代理物体？


## 第四步：创建支持IndirectDraw的草叶Shader

### 1）新增HLSL端：

新增数学库：全新的 Shader 引用文件，专门提供贝塞尔曲线的数学计算功能。

- **`CubicBezier` 函数**：给定 4 个控制点（p0 到 p3）和一个进度值 `t`（0到1），就能算出曲线上对应的 3D 坐标。这就为后续 Shader 让草叶产生平滑弯曲提供了数学依据。![[草地-030.png|535]]

- **`CubicBezierTangent` 函数**：用来计算曲线上某一点的切线方向。知道切线后，才能算出正确的法线，从而让弯曲的草叶产生正确的受光效果。![[草地-031.png|561]]

### 2）C#端：

因为我们接下来的顶点着色器将使用贝塞尔曲线算法**现场实时计算**顶点位置，不再需要从 CPU 这里要死板的顶点坐标了，所以需要去掉之前无用的位置缓冲区，并把所有的“零件缓冲区”对接给材质。


1.移除顶点坐标缓冲
	接下来的顶点着色器将使用贝塞尔曲线算法**现场实时计算**顶点位置，不再需要从 CPU 这里要死板的顶点坐标了

2.ArgsBuffer 扩容
	将间接绘制指令的数组长度从 4 改为 5，这是在处理包含实例偏移时的标准参数结构（5 个参数）

3.绑定材质
	在 `CreateComputeBuffersForMesh` 末尾新增四行关键代码：![[草地-032.png]]
	把 C# 拆解好的三角形顺序、颜色、UV 以及 Compute Shader 算出来的坐标名单，统统插进材质（着色器）的接口里
### 3）Shader端：

！舍弃从MVP矩阵旋转缩放位置的方式，转而使用我们在Cs中计算出来的位置信息 ！

1.定义
- 接收数据
	- 声明：`StructuredBuffer` 来接收 C# 传来的 `_GrassBlades`（位置）、`Triangles`（三角形）、`Colors`（颜色）和 `Uvs`（贴图坐标）
- 属性面板开放
	- 暴漏高度（Height）、倾斜（Tilt）、宽度（BladeWidth）、渐变色以及贴图等参数供外部调节

2.Pass中
- 增加BladeBuffer的结构定义，还有三角形、颜色、UV Buffer![[草地-033.png|536]]
- 数据传递
	- 模型数据到顶点：替换成顶点ID和实例ID![[草地-034.png|390]]
	- 

3.顶点着色器 （拿着贝塞尔公式，在坐标点现场“拔”出一根根弯曲的草）
- 使用顶点ID来获取索引Buffer的顶点Index![[草地-035.png]]
- 用这个Index来从颜色Buffer以及UVBuffer中获取数据![[草地-036.png]]
- 用新的顶点色变量来替换源代码中涉及到顶点色的地方，之前计算模型空间的地方改成直接计算世界空间的位置![[草地-037.png]]
	为什么这里加了一个 **blade.position** 就是世界坐标？
		因为在程序化生成中，**草根本没有挂载到任何 GameObject 上，也没有 `Transform` 组件，所以根本不存在什么“模型矩阵”**。我们必须手动完成这种转换，而在这里，转换的本质极其简单，就是**纯粹的位置平移**：
	- **草根的世界坐标 + 顶点相对于草根的偏移量 = 顶点的真实世界坐标**
	- 计算齐次坐标处：记得把模型坐标转换到齐次空间转换为世界坐标![[草地-038.png]]
- 1.通过 `Colors[positionIndex]` 的 R 通道拿到了顶点的高低进度 `t`（草根是0，草尖是1）
- 2.根据面板参数和 `t`，调用 `CubicBezier` 算出当前顶点的弯曲高度，然后结合基础的宽度和位置偏移，**实时“算”出这个顶点的最终 3D 坐标 (`worldPos`)**
- 3.同时算出了平滑的曲面法线，让一根平面的草看起来有圆柱弧度（`_CurvedNormalAmount`）

坑：.....![[草地-039.png]]


4.片元着色器 （责给这些草打光上色）
- 1.混合了草根到草尖的渐变色与外部贴图颜色
- 2.实现了基础的 PBR 光照（获取主光、计算阴影衰减、漫反射和简单的环境光 GI），最后输出有明暗立体感的草叶颜色



## 第五步：把草画在地形上

### 1）C#端：

1.引入地形对象
	新增了 `public Terrain terrain;` 变量，允许你在面板上把当前场景的 Terrain 拖拽进去![[草地-040.png|413]]

2.注册新的数据通道
	在底部的 ID 列表中，新增了用于传递高度图（`heightMapID`）、细节贴图（`detailMapID`）、地形坐标与缩放比例的一系列 ID![[草地-041.png]]
		最后两个，主要用于将世界空间的位置变换到高度图的UV坐标

3.提取并传递贴图
	在 `SetupComputeShader` 中，一旦检测到有地形，就会把两张极其关键的贴图塞给 Compute Shader：![[草地-042.png]]


### 2）Cs端：

1.引入核心库
![[草地-043.png]]
	为了调用解析高度图的现成函数（`UnpackHeightmap`）

2.写一个函数来获取UV坐标对应的地形世界空间的高度
![[草地-044.png]]
	 首先对高度图进行采样，PC\MAC可以直接获取R通道的值在去做缩放运算，移动平台这样做就会出问题，所以正确的方法：
		 1）包含Common文件![[草地-045.png]]
		 2）使用UnpackHeightmap_HeightMap.这个函数来获取高度值，函数内部会根据平台做相应结算

老师说 * 2 才拿到了正确值


3.写一个函数来获取UV位置草的图层值![[草地-046.png]]
	用第一个图层作为草的图层，所以值我们取r通道，后面我们根据这个值来确定这里有没有草


2.线程组核函数

![[草地-047.png]]
- 1）计算归一化坐标 （红框）
	将位置 - 地形的平面位置 = 相对位置
	相对位置 / 缩放值 = Heightmap对应的UV
	
	草地原本算出的坐标是 3D 世界里的绝对物理坐标（比如 X=50, Z=120），但读取贴图必须用 0 到 1 之间的 UV 坐标

- 2）遮罩剔除 （第一行白框）
	用刚才写的函数来判断，在UV对应的位置，草图层的混合权重是否大于0.5（0.5可以根据情况设置，它是一个阈、fa值？小于它，退出，所以就不会把这个位置加入最重要渲染的草叶的位置Buffer中）
	
  这里用 `SampleGrass` 读取了刚才 C# 传过来的地形涂层贴图（Detail Map）。如果当前坐标对应的贴图颜色低于 0.5（说明这里是石头路，没画草），线程直接 `return` 结束工作。**这根草不仅不会被 Append 进名单，连算高度的性能都省了！** 这是非常高级且高效的 GPU 剔除技巧

- 3）读取高度 （第二行白框）
	  对于通过了遮罩检查的草，利用 `SampleHeight` 去高度图（Height Map）里采样出当前的灰度值，算出这块地的真实海拔，最后强行修改草根的 `position.y`。现在，草终于老老实实地贴在山坡上了


## 第六步：基于距离和视锥的剔除优化

### 1）C#端：
为了支持剔除算法，C# 必须把相机的状态和剔除的规则传给 GPU

1.定义
	增加Cs端那边对应的变量![[草地-048.png|491]]

2.注册新的 Shader 变量 ID
![[草地-049.png]]

3.计算并传递矩阵数据

在`SetupComputeShader` 中，增加了将刚才那些距离剔除的阈值传给 Compute Shader 的代码，并且：
- 把**主摄像机的世界坐标**传给 GPU，用于算距离。
- 利用 `GL.GetGPUProjectionMatrix` 和 `cam.worldToCameraMatrix`，算出了摄像机的 **VP（View-Projection）矩阵**，并把它传给了 GPU。这个矩阵非常关键，它是用来把 3D 世界坐标转换到摄像机 2D 屏幕裁剪空间的数学工具。
![[草地-050.png]]
注意顺序，先和View矩阵相乘再乘上Project矩阵

### 2）Cs端：

原本只会“算好就 Append”的线程，现在要经过极其严格的重重安检。

1.定义

1)距离剔除：
- 这里的距离是摄像机的距离，所以我们需要摄像机的位置
- 距离的范围需要有一个起始位置和结束位置
- 在距离剔除的末端，我们可以控制一个0到1范围内的密度比例值，让草不至于完全消失
2)视锥剔除：
- 一个可以把世界空间位置转换到裁剪空间的矩阵
- 近裁剪面和左右边界的偏移值![[草地-051.png]]

2.新增函数 1：`DistanceCull` (距离剔除)
	草离镜头越远，长得越稀疏，太远直接不长![[草地-052.png|665]]
	 SmoothStep函数：得到一个当前距离在距离剔除范围内的一个平滑插值数，按照距离从近到远，它的值从0到1变化，但是因为我们做了1-的计算，所以最终的值是1到0的变化
	 distanceSmoothStep（里面那个）：从1到0变换到从1到最小草叶密度
	 distanceSmoothStep（外面那个）：用 **0到1之间的随机数** 和 **1 - 这个最小草叶密度** 来比较。>0 这里有草
	 
我靠这里好绕....


3.新增函数 2：`FrustumCull` (视锥体剔除)![[草地-053.png|442]]
	z (Unity默认负值) > w  比近裁剪面还要近  。< 0 远裁剪面还要远
	x 在 - w 和 w 之外，表示超出了左右或者上下边界
	因为是草叶，如果用一个点来判断，在边界位置可能会出现缝隙，所以我们在左右侧或者在视口的下端，增加一个少量的偏移值（_FrustumCullEdgeOffset）来修复这个问题。给负值就能解决缝隙问题。![[草地-054.png]]


这里好好理解一下，估计你还是没听懂....


4.线程核函数内：
	现在的 `Main` 函数在算好草贴地后的位置后，并不急着 Append，而是先让它过这两道安检：![[草地-055.png]]
		调用上面两个函数来计算是否需要剔除，没有被剔除的草才会被加入BladeBuffer


## 第七步：Voronoi分布图

所有的点到该区域种子点的距离，都小于到任何其他种子点的距离

r通道记录草丛的种类ID 
gb通道记录每个区域中心位置

传给草丛计算造型

### 1）Shader端：

1.定义

草丛的种类（对应一个草丛配置）
数量（Voronoi图的色块数量）
![[草地-056.png]]


2.Pass中

1）渲染状态：不裁剪正反面，不写入深度，不检测深度（直接把图像画出来）
![[草地-057.png|482]]
2）增加一个二维随机数生成的函数![[草地-058.png]]
	每个分量都在0到1之间


3.顶点着色器

1）用UV坐标当作每个草实例的平面坐标，去跟种子点进行距离判断，找出离得最近的那个种子点
![[草地-059.png]]
- 初始化最小距离、代表类型的草丛id、最近的种子点坐标![[草地-060.png]]
- 先拿种子点个数与最大安全阈值比较，确保for循环数不会太大![[草地-061.png]]
- for循环种子点，来遍历最小的距离值![[草地-062.png]]


### 2）C#端（测试脚本）：

1.增加成员变量![[草地-063.png|607]]

2.在Start函数中创建渲染纹理![[草地-064.png]]
使用指定的宽度和高度，深度缓冲设为0
ARGBHalf格式提供更好的精度，同时能支持一些移动设备，线性色彩空间计算出来的颜色不会被进行颜色转换

3.释放代码![[草地-065.png|646]]

4.Update函数更新材质的两个Shader常量![[草地-066.png|575]]
	这里使用Blit函数，把颜色渲染到我们创建好的渲染纹理RT中

5.利用GUI.DrawTexture把RT画在屏幕上让我们观察![[草地-067.png]]



## 第八步：Clumping

### 1）新增C#（Clump）端：

1.定义`ClumpParameters`结构体![[草地-068.png|670]]

### 2）C#（通用）端：

1.增加Clumping变量 ![[草地-069.png|557]]
- `clumpParameters` 是一个**在 Inspector 里配置的「草丛类型」列表**。就是上面那一坨结构体
 - `List<ClumpParameters>` 的作用：
列表长度 = **草丛的种类数**。第 0 项是一种草型，第 1 项是另一种……因为 struct 标了 `[Serializable]`，Unity 会在材质面板上把它渲染成可增删、可展开编辑的列表，不用写自定义 Editor。

2.增加字段ID![[草地-070.png]]
	1、3、4：传递给Cs计算
	2、：传递给Voronoi材质生成 Voronoi图片


3.增加草丛配置的ComputeBuffer、和用于生成Buffer的数组、Voronoi贴图![[草地-071.png]]

4.增加生成Voronoi贴图的函数![[草地-072.png]]
	设置好材质参数，然后Blit生成渲染纹理
	但是这次我们要把RT拷贝到一般的纹理上面，然后留给后面的Cs来使用


5.初始化函数中，调用纹理的创建函数CreateClumpTexture();

6.创建草丛Buffer![[草地-073.png]]
	个数对应配置容器里的个数，元素大小10个float是因为结构体有十个float

7.写一个函数把数组里的草丛数据拷贝到这个创建好的buffer中![[草地-074.png]]

8.释放函数，调用这两个![[草地-075.png|562]]![[草地-076.png|569]]



## 第九步：Clump效果给到Shader

### Cs端：

1.去掉草叶的高度、倾斜、宽度属性，因为我们将用BladeBuffer中的值来替代

2.在 GetP1P2 函数中加入 bend 参数，将它乘以 P1P2 的 Offset![[草地-077.png]]

3.新增函数，用罗德里格斯旋转公式，创建一个基于旋转轴和角的旋转矩阵![[草地-078.png|263]]

4.顶点着色器

- 把Blade的Buffer的读取放在顶部
- 获取Buffer中的弯曲、高、倾斜![[草地-079.png|566]]
- 把弯曲bend传入 P1P2 函数![[草地-080.png]]
- 高、倾斜、草叶宽度替代之前用到的属性值
- 修改之前获取世界坐标的地方 worldPos —— position ；blade.position —— centerPos
- 计算完法线和边缘法线之后，读取BladeBuffer中的angle和sideBend![[草地-081.png]]
	  一个是控制围绕Y轴的整体旋转，一个是用来在侧面观察草叶的时候让草有一个围绕Tangent方向的旋转。算出旋转矩阵：
	  ![[草地-082.png]]
- 处理sideBend效果![[草地-083.png]]
- 整体绕Y轴旋转效果![[草地-084.png]]
	  这里注释可能有误

开启后期处理，Camera使用TAA抗锯齿


## 第十步：2D Noise纹理生成

二维，带有三个独立通道的噪声纹理
采样噪声纹理的三个通道，来作为草叶局部的摆动方向

利用unity提供的API在cpu上生成噪声纹理

### C#端：

1.定义![[草地-085.png]]

2.生成函数![[草地-086.png]]![[草地-087.png]]

其中：
![[草地-088.png]]
	把计算出来的噪声值按照通道放进最终的颜色数组里面


再：
![[草地-089.png]]
- 通过SetPixels这个API将颜色数组设置给纹理，然后调用Apply接口来让纹理生效

3.加上生成新的随机种子的封装函数![[草地-090.png]]

4.导出函数![[草地-091.png]]
		要把纹理导出成图片文件，需要用到Texture2D的一个很有用的API：EncodeToPNG 得到一个bytes的数组，然后调用File类的WriteAllBytes保存到目标位置
			要用File类需要引入命名空间：using System.IO;
		如果是编辑器以内，就把资源刷新一下![[草地-092.png]]

5.在OnEnable()调用生成的函数![[草地-093.png]]


## 第十一步：风的动画

### 1）Cs端：

1.增加噪声贴图和采样器，风的尺寸、速度（这两个是对UV的变化），风的强度常量（影响草的弯曲，同时传递给vertexshader用来做动画）、草在风的作用下的旋转量、时间（动画效果）
![[草地-094.png|498]]

2.计算风的影响
	要在草叶的旋转赋值给bladebuffer之前，做风力的计算

- 风的直接影响

1）草叶位置作为初始风场采样uv![[草地-095.png]]
	  这样就有动画的效果了
- 注意 rotateBladeFromLocalWindAmount：如果我的风从草叶的侧面吹过来，它对草叶的旋转影响是很小的。如果是从正面，影响很（旋转）很大

2）风给草带来的额外弯曲![[草地-096.png|545]]

![[草地-097.png]]
	还需要把这个影响传递给bladebuffer的windForce（wf会拿来继续计算动画）

### 2）C#端：

1.传递变量![[草地-098.png|629]]

2.创建ShaderID![[草地-099.png|618]]

3.每帧往Cs里传数据   在SetupComputeShader()方法中![[草地-100.png|615]]


## 第十二步：Sine波动函数模拟摇摆动画

### Shader端：

在顶点shader中，通过控制 P2 和 P3 两个控制点的扭动，来增加草叶在风中凌乱的效果

1.增加动画属性![[草地-101.png|615]]

2.定义属性对应的shader常量![[草地-102.png|511]]

3.在P1P2函数中加上动画计算的代码
	先把函数名改为 P1P2P3，相应的在 P3 参数的前面加 inout 关键字，这样就可以改变传入的 P3 点.在参数列表中加入随机值和风力![[草地-103.png]]

- 在P1 P2 计算完成以后，计算风的影响增量
	- 对P2控制点的风力控制效果![[草地-104.png]]
		  初始是一个sine函数 乘以 风力
		  sine函数的参数，考虑时间，基于草叶位置随机的相位还有速度
		  加上0.66权重 乘以 2 PI倍数，因为这个P2不是最顶端的点，所以这个权重要小一点
		算完之后 乘以 权重、摆动幅度

	- 对P3![[草地-105.png]]
		- 最上面的点，权重设为1
		- 额外加上一个值，这个值是正比于1 - 弯曲值——弯曲越小的草叶，顶点运动幅度越大——越硬弯曲越小

- 最后加到P2 P3位置去![[草地-106.png]]

4.顶点着色器

1）获取baldebuffer的hash和windForce![[草地-107.png]]
 记得修改之前调用P1 P2 函数的地方


---

## 第十三步：Tile的草叶渲染

之前，在Terrin上按照一定的密度来生成bladebuffer，但如果是很大的地形，草又需要很密的话——bladebuffer就会很大，所以我们要让baldebuffer只用来生成摄像机周围区域的草叶

引入草区块概念：

我们把一个地形分成 n 乘 n 区块，只在摄像机附近的区块上生成草叶
![[草地-126.jpg]]
### 1）引入Tile概念：

1.定义：所在的地形（用来获取高度和覆盖信息）、包围盒（裁剪）、这个区块在地形中的格子位置 —— 用 Tile 来代替之前在整个地形上生成草叶![[草地-108.png]]


### 2）C#端：

1.定义变量： Tile的分辨率 和 边长上Tile 的个数![[草地-109.png|567]]

这里我们默认地形是长宽相等的，去掉老的代表整个地形的草叶分辨率

然后把草叶的间隔变成私有变量，现在需要由计算得出![[草地-110.png]]

ShaderID中新增Tile的位置![[草地-111.png]]

在私有变量中加入摄像机周围的草区块列表，将要进行渲染的草区块列表，和草区块的长宽尺寸


2.增加计算草叶间距的函数，并在Initialize()调用![[草地-112.png]]

3.修改bladebuffer的大小![[草地-113.png]]
bladebuffer的大小，由长宽的草叶分辨率乘积 再乘以最大可视区块的个数

4.写一个函数，更新摄像机周围16个区块的列表，计算区块的的长宽![[草地-114.png]]



双层for循环中，计算 4 乘 4 个区块编号偏移，加到摄像机区块编号上，得到摄像机周围的区块编号，如果编号超出地形边界，就忽略


5.增加函数：根据区块编号计算区块的包围盒

6.然后在for循环中生成区块放进周围的区块列表中
![[草地-115.png]]

7.写一个函数：判断周围区块哪些位于摄像机视锥中![[草地-116.png]]

8.写一个函数：判断一个包围盒是否在这六个面中![[草地-117.png]]
	注意：这个接口可能会出现：不在“视锥中的包围盒”这个结果判断成了在视锥体中（原因是：这六个面是无限大的，如果包围盒太大，又离视锥的角很近，就可能出现这种情况）
	巍峨避免这种情况出现：先控制生成的包围盒不要太大了

9.接着在之前的函数中遍历周围的区块，调用这个函数判断哪些应该送去渲染![[草地-118.png]]

10.写一个封装函数，更新需要渲染的区块![[草地-119.png]]

11.把之前更新Cs的一部分所有tile公用的数据提到最上面

12.把之前对整个Terrin更新一次Cs的函数改成以Tile为单位来更新Cs![[草地-120.png]]


13.在UpdateGpuParameters（）中
遍历循环中以Tile为单位调用Cs的计算![[草地-121.png]]

14.在OnDrawGizmos()函数中把需要渲染的Tile的包围盒显示出来
![[草地-122.png]]

15.在Update中调用对可视Tile的更新![[草地-123.png]]

### 3）Cs端：

1.增加属性
增加TilePosition，最大草丛数量


2.线程中

计算完Tile中的草叶位置之后，再加上Tile相对于Trren的偏移，这样才能得到草叶相对于地形的偏移
![[草地-124.png]]


在获取草丛编号之后做一个安全检查，让编号不超过最大值![[草地-125.png]]

现在不管周围地形多大，我们都只需要对周围的Tile进行Cs计算，来判断那里的草叶需要渲染了




## 第十四步：支持多地形和LOD的Tile生成

这节课我们将把Tile扩展到多个Terrin上 ，并且增加远处的低密度Tile

### 1）C#端：


1.定义

为了把 tele扩展到多个Terrin，首先我们增加一个 Listt容器来存放需要渲染草的地形![[草地-127.png|594]]

增加一个函数来收集场景中所有的地形![[草地-128.png|538]]

修改计算草间距的函数：我们这里假设所有地形都同样大，随便用第一个地形的尺寸来计算草间距![[草地-129.png]]


在 inisilize函数中要用地形收集函数。


在更新GrassTiles（）函数中对每个地形都调用收集摄像机周围的Tile的函数，这样我们就把 groshel从单地形扩展到了多地形。![[草地-130.png]]


我们在之前的4 X 4 Tile 外层再加一圈大的 Tile，使标准 Tile 的两倍大，草的数量一样，所以草的密度就会是之前的一半
![[草地-131.jpg]]

为了让 Tile l支持这两种尺寸和草密度，我们对 Tile 结构体在增加三个成员变量，分别是：草间距乘积因子和两个方向上的草分辨率除法因子![[草地-132.png]]

为什么要分两个方向我们后面再讲

我们再完善构造函数![[草地-133.png]]

因为 Tile 的分辨率有了两个方向的除法因子 ，所以对应的传给 compushader的 ID也要有两个。![[草地-134.png]] 

由于我们在之前 4×4的太要外层再加了一圈 ，所以太偶生成这的函数就需要改造首先我们只关心包含摄像机周围发成八范围内 tele的地形远处的地形就不用生成太阳了。![[草地-135.png]]

然后我们创建一个开始展HashSet来记录外层那些需要合并成大的 Tile 起点位置![[草地-136.png]]为什么要用 HashSet 呢 ？因为它的值是唯一的，而在合并 Tile 的时候，四个标准 Tile 对应着合并后大的 Tile 的同一个起点位置，所以用集合就可以去除相同的位置信息。 


然后我们写双层的 for 循环来建立摄像机周围 8×8 个 Tile
![[草地-137.png]]我们需要确定哪些是中心的标准 Tile，这些 Tile 跟上一大步一样直接生成![[草地-138.png]]

所以我们写一个判断是否是标准 Tile 的函数 ，它其实就是判断是否是在摄像机周围 4 X 4 的范围![[草地-139.png]]
再写个函数判断这个 Tile 的位置是否处于当前地形内![[草地-140.png]]

再写一个函数，跟上一大步的实现方式一样，把这些中心的 Tile 直接生成![[草地-141.png]]

有了这三个函数 ，我们就在双层 for 循环中把中心区域的 Tile 找出并生成出来![[草地-142.png]]

接下来，稍微麻烦一点的就要合并外圈的 Tile：我们写个函数计算 Tile 是否需要被合并成大 Tile 。合并后的 Tile 的起始位置又在哪里？
![[草地-143.png]]

需要合并的分四种情况：前两种是 Tile 处于左右两边，这两种情况 X 的起始位置是唯一的，所以需要用 2 的倍数来计算.

而后两种是 Tile 属于上下两排，这种情况跟前两种情况相反，Z 其实位置是确定的，但是 X 需要计算。

我们在 for 循环中把计算出来的合并的起始位置放进集合里，待会儿我们通过这个集合来生成合并后的 Tile。![[草地-144.png]]

 但在此之前，我们要把生成 tileBounds 的函数修改一下，前面我们提到两个方向的分辨率不一样相应的两个方向的尺寸可能也不一样，所以我们给这个生成 Bounds 的函数增加两个方向尺寸的倍数。
 
 可能现在大家已经猜到了，造成两个方向草叶分辨率和 Tile 尺寸不一样的原因就是被边缘裁剪了。造成任何一个方向的尺寸和分辨率都有可能变成原先的一半。
 
 我们先增加和调用这个通过合并后的位置生成合并 Tile 的函数![[草地-145.png]]
 
 接下来我们来实现这个函数：首先在 for 循环中把超出地图的 Tile 位置忽略， 然后我们初始化分辨率除法因子和其始位置，然后我们设置计算左右上下被地形边缘裁掉一半之后的分辨率除法因子和新的起始位置然后创建对应的Tile。![[草地-146.png]]
 
 然后我们来修改 computeshader 的 dispatch 的地方：因为这里 dispatch 的线程数是根据两个方向的分辨率除以 8 来算的，所以在分辨率有除法因子的情况下，线程数的计算也需要把这个因素考虑进去。![[草地-147.png]]
 
 最后我们修改给 computeshader 传递参数的地方：我们把两个方向的分辨率通过除法因子计算了之后分别传进去。然后再计算应用了缩放的草叶间距![[草地-148.png]]
![[草地-149.png]]
 
 相应的 computeshader 也要修改一下，把分辨率变成两个。![[草地-150.png]]然后在入口函数中判断分辨率数量的地方，分别考虑两个方向的分辨率就改好了![[草地-151.png]]
	 原来没有X Y现在加了X Y
 
 好了现在我们可以在多个地形之间移动摄像机观察草地的生成了。 


---

## 第十五步：不同LOD Tile之间的平滑过渡


上一步我们在中心区域 Tile 的周围增加了一圈低密度的 Tile，这样把草的区域放大的同时，带来了一个新的问题：就是不同密度的 Tile 之间由于没有过度造成了视觉上的突兀。

这是我们不想看到的，好在我们可以通过 computeshader 中距离裁剪的密度衰减来处理这个问题。

我们回到 C# 代码：在 Culling 那几个变量中找到距离衰减的开始距离、结束距离以及衰减镜头的密度。 我们希望分开控制两个 LOD 级别的 Tile，所以我们把衰减距离做成两份分别给 LOD0 也就是中心区域，和 LOD1 也就是外围区域使用。![[草地-152.png]]
	StartDistance —— LOD0 ......


末端的衰减密度我们现在就不能手动设置了 ，所以删掉。
![[草地-153.png]]

我们回想外围的密度是那圈密度的四分之一，所以 LOD0 对应的衰减密度就应该是四分之一，这样才能和未发生衰减的外圈无缝衔接。

而外圈的衰减密度应该是0，这样在远处的草就会渐渐消失。

我们找到给 computeshader 设置参数的地方：![[草地-154.png]]
删掉原先这三个参数，然后判断当草叶间距倍数是1，也就是内圈。

我们设置内圈的衰减距离参数和末端衰减密度为 025
else：外圈则设置为外圈对应的参数

现在我们回到编辑器测试我们已经把地形增加了许多个用来测试远距离的草地效果。

这里我通过 camera 的点裁剪面的位置来估计草叶衰显的距离。我把那圈的最远衰减距离设置为 80外圈从 120 开始衰减到 200 的距离衰减为零。 

这里如果我们把 200 这个值增大则会出现距离被 Tile 边界裁剪的问题 ，所以不能太大

看到两级 LOD 没有明显的边界，远处的草有合理的消失之后，我们就可以在 game 窗口观察效果了。好了 ，现在我们的草叶从进到远力度逐渐降低的变化已经是连续的了。