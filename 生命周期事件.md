时间轴 →  ───────────────  游戏对象存活期  ──────────────────→

  加载/创建时                    每一帧(播放中)              销毁/停止时
  Awake()                       Update()                    OnDestroy()
    │                             │                            │
    └─ Initialize()                └─ UpdateGpuParameters()      └─ DisposeBuffers()
         └─ 开显存 buffer              └─ 填草数据进 buffer           └─ 释放显存


- **Awake** 只在"对象被创建那一刻"跑一次 → 开显存。
- **Update** 之后每一帧都被调用 → 反复复用那块显存填数据。
- **OnDestroy** 只在"对象被销毁 / 场景卸载 / 停止播放"那一刻才跑 → 释放显存。