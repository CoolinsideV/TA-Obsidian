

> [!info] 深度缓冲的本质
> 
> 深度缓冲 (Depth Buffer) 是一张与 Render Target 分辨率一致的灰度贴图，专用于记录当前像素距离相机的深度值。在光栅化阶段，相机空间到 NDC（归一化设备坐标）的深度映射是非线性的，其核心数学公式为：
> 
> $$Z_{ndc} = \frac{z_{far} + z_{near}}{z_{far} - z_{near}} + \frac{1}{z} \left( \frac{-2 \cdot z_{far} \cdot z_{near}}{z_{far} - z_{near}} \right)$$

## 1. 深度测试 (ZTest) 与写入 (ZWrite) 的协同

管线在输出最终像素前，必须依次执行以下两个深度操作：

- **ZTest (深度测试)**: 将当前执行的片元深度 $Z_{current}$ 与缓冲中已有的 $Z_{buffer}$ 对比。默认状态为 `LEqual`（小于等于，即距离更近或相等则通过）。
    
- **ZWrite (深度写入)**: 当且仅当 ZTest 通过时，若当前 Pass 开启了 `ZWrite On`，GPU 才会用 $Z_{current}$ 覆写 $Z_{buffer}$ 中的旧值。
    

## 2. 不透明物体：为何必须开启 (`ZWrite On`)？

对于常规实体几何体（Opaque Queue），深度写入是确立三维空间正确遮挡关系的基石。

- **物理遮挡确立**: 写入深度后，无论模型以何种无序状态提交给 GPU，最终画面都能保证近处物体正确遮挡远处物体。
    
- **Early-Z (提前深度剔除)**: 现代图形 API 支持在片元着色器执行**前**进行深度测试。开启深度写入后，管线通常将不透明物体按**从前向后 (Front-to-Back)** 的顺序渲染，被挡住的像素会被直接丢弃，极大节省了光照计算性能。
    
- **防止 Overdraw**: 严格的深度写入是降低像素过度绘制 (Overdraw) 的最有效手段。
    

## 3. 半透明材质：为何必须关闭 (`ZWrite Off`)？

半透明物体（Transparent Queue，如水体、玻璃、粒子特效）面临着“既要透出底色，又不能阻挡同类”的渲染悖论。

- **深度遮挡异常**: 如果前方的玻璃开启 `ZWrite On`，它写入的深度会导致管线直接剔除玻璃后方的其他几何体。视觉上，玻璃背后的特效或模型会凭空消失。
    
- **画家算法 (Painter's Algorithm)**: 渲染半透明必须关闭深度写入，并严格按照**从后向前 (Back-to-Front)** 的顺序依次渲染叠加。
    
- **只读不写机制**: 关闭写入后，玻璃依然能通过 ZTest 识别墙壁的遮挡，但自身不产生深度屏障，允许后续的混合方程式 ($C_{src} \times A + C_{dst} \times (1-A)$) 正确执行。
    

## 4. 管线中的高级 ZWrite 技巧

技术美术在构建复杂 Shader 时，常通过组合管线状态来实现特殊效果：

|**技术方向**|**管线状态组合**|**核心原理与应用场景**|
|---|---|---|
|**Pre-Z (深度预计算)**|`ColorMask 0`<br><br>  <br><br>`ZWrite On`|先用极低成本（无光照）将复杂模型轮廓写入深度。后续完整渲染时设为 `ZTest Equal`，确保每个像素只执行一次昂贵的着色器代码。|
|**X-Ray / 遮挡透视**|`ZTest Greater`<br><br>  <br><br>`ZWrite Off`|增加一个 Pass，仅当模型被深度缓冲遮挡（深度大于当前缓冲）时执行，常用于绘制隔墙透视的单色剪影或描边。|
|**贴花系统 (Decal)**|`ZTest LEqual`<br><br>  <br><br>`ZWrite Off`|紧贴表面的弹孔、法线细节。若写入深度，非线性深度公式产生的浮点精度误差会引发严重的 Z-Fighting (深度闪烁)。|