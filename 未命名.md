# 头发渲染理论与Shader实践：Kajiya-Kay 与 Marschner 模型

> [!info] 导读
> 
> 渲染真实的头发一直是图形学和 Shader 编写中的难点。普通的基于物理渲染（PBR）材质或者传统的 Blinn-Phong 模型放在头发上，往往看起来像是一根根塑料管或者是一坨铁块。本文重点拆解最经典的两个头发渲染模型：**Kajiya-Kay 模型**（老牌经典，适用于移动端或卡通渲染）和 **Marschner 模型**（现代主流，适用于 3A 级别写实渲染）。

## 一、 为什么普通的 Blinn-Phong 没法渲染头发？

要理解头发渲染，首先需要区分两个概念：

- **各向同性（Isotropic）：** 普通表面（如台球），光打在上面高光是一个**圆点**。无论你怎么旋转这个球，高光点都在那里。传统的 Blinn-Phong 模型就是通过法线（Normal）和灯光计算出这个高光点。
    
- **各向异性（Anisotropic）：** 头发不是一个平面，而是一根极其细微的**圆柱体**。当无数根头发排列在一起时，光线打在上面不会形成一个点，而是会连成一个**高光环（光圈，俗称“天使环”）**。
    

如果我们在 Shader 里强行给头发赋予法线（$N$），因为头发太细小，法线变化极其剧烈，渲染出来的结果会全是噪点。因此，**我们需要一种“不依赖法线，而是依赖头发走向（切线）”的算法**。

## 二、 Kajiya-Kay 模型：把头发当做不透明的圆柱

Kajiya-Kay 模型诞生于1989年，它最大的贡献是：**抛弃了法线（$N$），引入了切线（$T$）参与光照计算。** 在这个模型中，我们将单根头发视为一根完全不透明的光滑圆柱体。

### 1. 漫反射（Diffuse）

在传统的 Lambert 漫反射中，我们用的是法线与光线的点乘：$N \cdot L$。

但在 Kajiya-Kay 中，我们计算的是**光线（$L$）与头发切线（$T$）夹角的正弦值**。

在 Shader 中，可以通过点乘推导正弦值（利用 $\sin^2\theta + \cos^2\theta = 1$）：

$$Diffuse = \sin(T, L) = \sqrt{1 - (T \cdot L)^2}$$

### 2. 高光（Specular）与半角向量

为了制造出“天使环”的各向异性高光，我们在实际的游戏引擎（如 Unity）应用中，通常会使用半角向量（Half Vector，$H$）来进行优化近似：

$$Specular = (\sin(T, H))^{exponent} = (\sqrt{1 - (T \cdot H)^2})^{exponent}$$

### 3. 毛鳞片切线偏移（Tangent Shift）

> [!tip] 核心技巧
> 
> 真实的头发表面并不是完全光滑的，表面布满像瓦片一样朝向发梢的**毛鳞片**。这会导致高光发生偏移。为了模拟这种错落有致的双层高光（Primary & Secondary Highlight），我们需要在 Shader 中通过一张灰度贴图（Shift Map）结合法线方向，将切线 $T$ 稍微“掰弯”一点。

### 4. HLSL 代码实践

以下是一个标准的包含**双层高光**和**毛鳞片切线偏移**的 Kajiya-Kay 高光计算函数：

High-level shader language

```
// 1. 切线偏移函数：根据贴图采样值将切线沿法线方向偏移
float3 ShiftTangent(float3 T, float3 N, float shiftAmount) 
{
    // shiftAmount 通常是从 Shift Map 中采样得到的值，并映射到 [-1, 1] 区间
    return normalize(T + N * shiftAmount);
}

// 2. Kajiya-Kay 各向异性高光计算（核心算法）
float StrandSpecular(float3 T, float3 V, float3 L, float exponent) 
{
    float3 H = normalize(L + V);
    float dotTH = dot(T, H);
    
    // 利用三角函数恒等式 sin(x) = sqrt(1 - cos^2(x))
    // 注意：为了防止精度问题导致 dotTH*dotTH 大于 1，通常需要 clamp 或者 max 处理
    float sinTH = sqrt(max(0.0, 1.0 - dotTH * dotTH));
    
    // 也可以通过一层平滑处理方向，这里直接求幂
    return pow(max(0.0, sinTH), exponent);
}

// 3. 在 Fragment Shader 中的组合调用（伪代码流程）
float4 frag(v2f i) : SV_Target 
{
    // 基础向量
    float3 T = normalize(i.tangent);
    float3 N = normalize(i.normal);
    float3 V = normalize(_WorldSpaceCameraPos - i.worldPos);
    float3 L = normalize(_WorldSpaceLightPos0.xyz);
    
    // 采样偏移贴图 (Shift Map 存的是灰度值)
    float shiftTex = tex2D(_ShiftMap, i.uv).r; 
    // 将 0~1 映射到 -1~1，并乘以强度的控制参数
    float t1Shift = (shiftTex - 0.5) * _PrimaryShiftIntensity;
    float t2Shift = (shiftTex - 0.5) * _SecondaryShiftIntensity;
    
    // 计算两层偏移后的新切线
    float3 t1 = ShiftTangent(T, N, t1Shift);
    float3 t2 = ShiftTangent(T, N, t2Shift);
    
    // 第一层高光（直接反射，通常是光源颜色，比如纯白）
    float spec1 = StrandSpecular(t1, V, L, _PrimaryExponent) * _PrimarySpecularColor;
    
    // 第二层高光（内部折射带出的颜色，通常带有头发底色，更加发散）
    float spec2 = StrandSpecular(t2, V, L, _SecondaryExponent) * _SecondarySpecularColor;
    
    // 混合输出
    return (spec1 + spec2) * lightColor;
}
```

## 三、 Marschner 模型：真实毛鳞片与光线追踪

Kajiya-Kay 把头发当成了“塑料细丝”，认为光照上去后只发生了表面反射。但在现实中，光线是会**穿透**头发的，这就引出了更为硬核的 **Marschner 模型**。

它是基于真实的物理测量得出的，目前是 UE5 级别高级毛发渲染的“黄金标准”。Marschner 模型将头发当成**半透明的、表面布满毛鳞片（Cuticles）的电介质圆柱体**。

### 1. 核心概念：三条光线路径（BCSDF）

Marschner 用 **BCSDF**（双向曲线散射分布函数）取代了传统的 BRDF。它将物理现象分解为三条路径：

- **R (Reflectance - 表面反射)：**
    
    - **现象：** 光线直接在头发表面反射。
        
    - **视觉表现：** 产生**第一层高光（Primary Highlight）**。由于毛鳞片像屋顶的瓦片一样是指向发梢的（倾斜角 $\alpha$ 通常在 $2^\circ$ 到 $3^\circ$），这导致反射光线会偏向发根（Root）。
        
- **TT (Transmit-Transmit - 两次透射)：**
    
    - **现象：** 光线穿透头发，从另一侧射出（前向散射）。
        
    - **视觉表现：** 产生**透光边缘**。这是背光时头发边缘会“发亮、发丝根根分明”的原因。
        
- **TRT (Transmit-Reflect-Transmit - 透射-内部反射-透射)：**
    
    - **现象：** 光线射入头发内部，撞击到内壁发生全反射，然后再从正面射出。
        
    - **视觉表现：** 产生**第二层高光（Secondary Highlight）**。因为光线在内部穿梭过，吸收了色素，所以这层高光带有头发的本色。它会偏向发梢，并且由于折射率（IOR）原因，比第一层更柔和。
        

### 2. 纵向与横向计算（数学拆解）

在 Shader 实现 Marschner 模型时，它将散射函数 $S$ 拆解为两个维度的乘积相加：

$$S(\theta_i, \theta_r, \phi_i, \phi_r) = \sum_{p \in \{R, TT, TRT\}} M_p(\theta_i, \theta_r) \cdot N_p(\phi_i, \phi_r)$$

- **纵向散射函数 $M_p$ (Longitudinal Function)：**
    
    处理沿头发长度方向（切线方向）的光线分布。通常使用高斯分布（Gaussian distribution）来模拟粗糙度引起的散射。引入毛鳞片倾角 $\alpha$ 后，R路径偏移 $-\alpha$，TRT路径偏移 $+\alpha$。
    
- **横向散射函数 $N_p$ (Azimuthal Function)：**
    
    处理垂直于头发截面（环绕圆柱体）的光线分布。这里需要极其复杂的菲涅尔方程（Fresnel Equations）和能量守恒计算，去追踪光线在圆形截面内部折射的衰减。
    

> [!warning] 落地工程的妥协
> 
> 纯粹的 Marschner 模型计算量极其庞大（尤其是 $N_p$ 涉及复杂的折射衰减计算）。在实际的游戏渲染（如 UE 的 Hair 材质）中，图形程序员通常会将 $N_p$ 的结果预计算并烘焙到一张 **LUT（查找表贴图）** 中。在 Fragment Shader 阶段，直接通过视角、光照夹角和粗糙度去采样 LUT，从而实现物理准确度与性能的平衡。